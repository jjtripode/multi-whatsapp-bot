export interface WtsContact {
    id: string;
    name: string;
    // phone: string
    // email?: string
    // lastMessage: string
    // lastContact: string
    // status: "active" | "blocked" | "archived"
    // tags: string[]
    // totalMessages: number
    // avatar?: string
}